<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.mapper.AdministratorsMappper">

    <select id="Sign_in" resultType="Administrators">
        select *from administrators where name=#{name} and password=#{password}
    </select>

</mapper>